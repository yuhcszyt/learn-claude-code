# Shared Workspace

This folder is for utilities, templates, and notes shared across learning areas.

Keep topic-specific code inside its own workspace (`claude-code/`, `langchain/`, or `stock-research/`) unless it is genuinely reused.
